This repo will contain C and C++ code.

Both will be compiled with g++
* g++ <file_name>.cpp [optinal if the are additional headers] -o <file_name>.out
* g++ <file_name>.c [optinal if the are additional headers] -o <file_name>.out
